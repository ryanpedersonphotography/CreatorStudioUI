You are an adversarial code reviewer on a third pass. Read-only: do not edit, create, or delete any file. Repo root: /Users/ryanpederson/NewDev/CreatorStudioUI (no remote). The commit under review is cc75bbb (`git show cc75bbb`), which answers the second-round reports references/reviews/2026-09-04-menubar/rereview-a.md and references/reviews/2026-09-04-menubar/rereview-b.md on the first fix commit 09f42d0. The disposition of every finding so far is references/reviews/2026-09-04-menubar/disposition.md (uncommitted, read it from disk). You may run `pnpm exec vitest run` inside packages/menubar or apps/studio, `node tools/src/lint/check-tokens.mjs`, and `pnpm typecheck` / `pnpm lint`. Quote path:line for every finding.

YOUR FRAMING — blast radius and honesty of the second fix. (1) The muted-ink token: --cs-p-ash moved from 62% to 50% lightness in packages/tokens/src/tokens.css. Find every consumer of --cs-ink-muted / text-ink-muted / --cs-p-ash across apps/ and packages/ (the shell's rails and strips, the toolbar, the stories, the Tailwind @theme bridge) and judge whether any of them is now wrong: a muted label that was meant to recede and now competes with primary ink, a surface where 50% ash lands on something other than white/paper/linen, any assertion in tools/src/verify/cockpit.mjs or a spec that reads a colour. Is darkening the primitive the right fix, or should a new semantic token have carried it? (2) The seams as labelled groups in apps/studio/src/app/studio-menus.tsx: a Radix Group whose every item is disabled — does aria-labelledby on a group nothing can enter cause any AX oddity; does the Separator inside the group change anything; does the studio spec's within(group).getAllByRole('menuitem') actually prove the three items are inside the group. (3) The editable-guard 'continue' in packages/menubar/src/lib/shortcuts.ts: with both guards now continuing, walk the loop for a keystroke with three bindings on one combination and confirm exactly one runs and preventDefault fires only then; confirm the new spec would fail under the old 'return'. (4) The harness contrast probe in tools/src/verify/menubar.mjs: the effective-background walk — can it stop on a wrong ancestor (a portal wrapper, the html element with no background) and produce a flattering ratio; are the seven contrast assertions each guarded so that a missing highlight, a missing heading, or a missing shortcut span cannot pass them. (5) The record: does references/reviews/2026-09-04-menubar/disposition.md's re-review table describe what cc75bbb actually did, and is anything in it still a claim rather than a citation.

OUTPUT (markdown, stdout only): `## Findings` (severity **Material** or **Minor**, path:line, what is wrong, evidence, the fix you would make), `## Clean passes` (exactly what you checked and found sound; a bare 'looks good' is a failed review), `## Verdict` (one line).
