You are an adversarial code reviewer. Read-only: do not edit, create, or delete any file. Repo root: /Users/ryanpederson/NewDev/CreatorStudioUI (no remote; commits 374e56e, 970a700, 070361b are the work under review — `git show --stat` each, `git diff fe0ee37..HEAD` for the whole diff).

The work: the studio's top shelf became a VS Code-style menu bar. Plan (approved, reviewed): /Users/ryanpederson/NewDev/CreatorStudioUI/references/reviews/2026-09-04-menubar/plan.md. Build under review:
- /Users/ryanpederson/NewDev/CreatorStudioUI/packages/menubar/ (portable package: src/lib/menubar.tsx, shortcuts.ts, menubar.css, specs, stories, README.md, package.json)
- /Users/ryanpederson/NewDev/CreatorStudioUI/apps/studio/src/app/ (studio-commands.ts, studio-menus.tsx, studio-shortcuts.tsx, studio-toolbar.tsx, use-theme.ts, studio-cockpit.tsx changes, app.tsx, main.tsx, specs), apps/studio/src/styles.css
- /Users/ryanpederson/NewDev/CreatorStudioUI/packages/contracts/src/lib/preferences.ts
- /Users/ryanpederson/NewDev/CreatorStudioUI/tools/src/verify/{lib,cockpit,menubar,all}.mjs, tools/src/vitest/setup.ts, root eslint.config.mjs (kind:portable), .ladle/preview.css
- Radix source for behaviour claims: node_modules/.pnpm/@radix-ui+react-menubar@*/node_modules/@radix-ui/react-menubar/dist/index.mjs and node_modules/.pnpm/@radix-ui+react-menu@*/node_modules/@radix-ui/react-menu/dist/index.mjs

YOUR FRAMING — correctness and buildability. Hunt for what is WRONG: code that does not do what its comment, the plan, the README or AGENTS.md says; a lifecycle or race bug (useShortcuts ref reads, useTheme's no-write-on-mount and its ref, the focus handoff's data-region check and its event order with Radix's FocusScope, the StudioShortcuts binder living outside the panels, MenubarSub's controlled open + Escape override interacting with Radix's hover timers and pointer grace); a11y regressions (roles, names, aria-hidden, aria-keyshortcuts, focus return); the reset command's key list vs what the shell actually writes (packages/shell/src/lib/cockpit.tsx, packages/contracts/src/lib/layout-store.ts); the CSS contract block vs the token lint (tools/src/lint/check-tokens.mjs) — any `token-ok` hiding a value that should be a token, any rule that breaks in the studio's dark theme (packages/tokens/src/tokens.css: highlight-fg maps to --cs-surface); the eslint constraint actually forbidding what it claims; harness assertions that could pass with the feature broken or absent, or that are timing-fragile. Two deliberate deviations from the plan need your verdict: (1) shortcuts bind in StudioShortcuts via a new `shortcuts` slot on StudioCockpit rather than in the toolbar; (2) Menubar.Sub overrides Escape to close one level (APG) where Radix closes the whole bar.

Verify claims against the world: run `pnpm exec vitest run` inside a package dir if it helps (read-only otherwise). Quote file:line for every finding.

OUTPUT (markdown, to stdout only): `## Findings` — each with severity **Material** (would ship a defect or a false claim) or **Minor**, `path:line`, what is wrong, the evidence, and the fix you would make; then `## Clean passes` — list exactly what you checked and found sound (a bare "looks good" is a failed review); then `## Verdict` one line.
