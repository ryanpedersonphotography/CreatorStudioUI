You are an adversarial code reviewer re-reviewing a FIX commit. Read-only: do not edit, create, or delete any file. Repo root: /Users/ryanpederson/NewDev/CreatorStudioUI (no remote). The fix commit is 09f42d0 (`git show 09f42d0` is the whole diff; its parent 070361b is the code the two first-round reviews looked at). The first-round reviews it answers: references/reviews/2026-09-04-menubar/review-a.md (correctness) and references/reviews/2026-09-04-menubar/review-b.md (gaps). Files touched: packages/menubar/src/lib/{menubar.tsx,menubar.css,shortcuts.ts,shortcuts.spec.ts,menubar.spec.tsx,menubar.stories.tsx}, packages/menubar/README.md, packages/tokens/src/tokens.css, apps/studio/src/styles.css, apps/studio/src/app/{studio-menus.tsx,studio-cockpit.stories.tsx}, tools/src/verify/menubar.mjs. You may run `pnpm exec vitest run` inside packages/menubar or apps/studio. Quote path:line for every finding. OUTPUT (markdown, stdout only): `## Findings` (severity **Material** or **Minor**, path:line, what is wrong, evidence, the fix you would make), `## Clean passes` (exactly what you checked and found sound; a bare 'looks good' is a failed review), `## Verdict` (one line).

YOUR FRAMING — does each fix actually close its finding, and does any fix break something? For every Material and Minor finding in review-a.md and review-b.md, find the hunk in 09f42d0 that answers it and judge it on the code, not the commit message: the max-height move (does the var() now resolve on the element Radix sets it on; is the 80vh fallback still right standalone); the contrast token (--cs-on-accent pinned to night in both themes: is it declared where the token lint and both theme blocks need it; does the bridge read it; would any other consumer of --cs-accent text be affected); the `continue` in useShortcuts (can two bindings now double-fire; is preventDefault still right); physicalCode for digits (what does macOS report for Option+digit; does Slash/punctuation now correctly NOT match); Menubar.Group's label/aria-labelledby (does the id land on the label; is a Group without a label unchanged; does the stories/spec fixture still cover Menubar.Label standalone); the "Coming soon" heading in File/Edit (does it change any accessible-name or item-count assumption in app.spec/studio-menus.spec/menubar.mjs); and every harness change in tools/src/verify/menubar.mjs (could the new max-height, contrast, Tab-out, dispatchEvent('click') and layout-key assertions pass with the fix reverted — reason from the code; the session reports each went red under a revert, check whether the assertion logic supports that).
