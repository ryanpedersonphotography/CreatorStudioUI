You are an adversarial code reviewer on a fifth, narrow pass. Read-only: do not edit, create, or delete any file. Repo root: /Users/ryanpederson/NewDev/CreatorStudioUI (no remote). The commit under review is c90ded1 (`git show c90ded1`), which answers the fourth-pass report references/reviews/2026-09-04-menubar/rereview-d.md on 2888f08. You may run `pnpm exec vitest run` inside packages/menubar or apps/studio, `pnpm typecheck` / `pnpm lint`, and `node tools/src/verify/menubar.mjs` against the dev server at http://localhost:5180 (it is running; do not start any server, and never open a browser window: Playwright here is headless). Quote path:line for every finding.

YOUR FRAMING — only whether rereview-d's three findings are closed by this commit, and whether it breaks anything. (1) D1: read the pressedState probe in tools/src/verify/menubar.mjs (search 'pressedState') and say what a regression would have to look like to slip past it now: the outline collapsing to the border colour, the outline moving to the unpressed button, the fill becoming the signal, a theme where the shelf paints something other than surface. Is 'borderTopColor' the right property (all four sides; the button has a uniform border)? Does the dark run happen with one toggle actually unpressed (the harness rails the nav by shortcut first) and is the toggle restored afterwards so the picture in §10 is unchanged? (2) D2: the outline is ink, not the accent the reviewer suggested; the commit says it measures higher and avoids four permanently orange chips. Confirm the numbers (16.0 light, 14.2 dark) by reading the probe's output or computing from packages/tokens/src/tokens.css, and confirm ink-on-fill legibility is unchanged. Say whether the ink choice has any downside the accent would not (e.g. a pressed chip now resembling a focused one: check the focus ring rule for the buttons in apps/studio/src/app/studio-toolbar.tsx and styles.css). (3) D3: the foreground alpha composite at the probe: is the order right (foreground over base), and can it ever lower a ratio for an opaque colour (it must not). (4) Anything the commit broke: cockpit.mjs assumptions, app.spec, the §10 screenshot geometry.

OUTPUT (markdown, stdout only): `## Findings` (severity **Material** or **Minor**, path:line, what is wrong, evidence, the fix you would make), `## Clean passes` (exactly what you checked and found sound; a bare 'looks good' is a failed review), `## Verdict` (one line).
