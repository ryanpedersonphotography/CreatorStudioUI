You are an adversarial code reviewer on a fourth, narrow pass. Read-only: do not edit, create, or delete any file. Repo root: /Users/ryanpederson/NewDev/CreatorStudioUI (no remote). The commit under review is 2888f08 (`git show 2888f08`), which answers the third-pass report references/reviews/2026-09-04-menubar/rereview-c.md on cc75bbb. You may run `pnpm exec vitest run` inside packages/menubar or apps/studio and `pnpm typecheck` / `pnpm lint`. Quote path:line for every finding.

YOUR FRAMING — only whether the two Material findings of rereview-c.md are closed by this commit, and whether the commit breaks anything. (1) Finding 1: the region toggle's pressed state now adds `aria-pressed:bg-surface-muted` in apps/studio/src/app/studio-toolbar.tsx. Is that utility real under this repo's Tailwind bridge (packages/tokens/src/tokens.css @theme inline block; Tailwind v4 aria-* variants)? Does it render in both themes with adequate contrast for the ink on it (ink on linen light, chalk on steel dark: compute)? Does the harness assertion in tools/src/verify/menubar.mjs (search 'pressed toggle carries a fill') actually discriminate: read it and say what a regression would have to look like to slip past it. Does any cockpit harness or spec assumption about the buttons change (tools/src/verify/cockpit.mjs reads aria-pressed; apps/studio/src/app/app.spec.tsx)? (2) Finding 2 (the record) is handled by the closing commit, not this one; skip it. (3) Minor 3 and 4: the contrast() rewrite in menubar.mjs — walk the compositing loop: does it composite in the right order (top layer over the composited layers below), does it stop at the first opaque layer, is `painted` false only when no ancestor paints at all, and does the white base ever mask a real dark-theme regression (reason about a dark page whose body paints an opaque colour)? Do the seven probes each require `painted` and, for the highlighted-row ones, `data-highlighted`?

OUTPUT (markdown, stdout only): `## Findings` (severity **Material** or **Minor**, path:line, what is wrong, evidence, the fix you would make), `## Clean passes` (exactly what you checked and found sound; a bare 'looks good' is a failed review), `## Verdict` (one line).
