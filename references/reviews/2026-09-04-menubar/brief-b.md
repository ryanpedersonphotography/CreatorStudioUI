You are an adversarial reviewer. Read-only: do not edit, create, or delete any file. Repo root: /Users/ryanpederson/NewDev/CreatorStudioUI (no remote; commits 374e56e, 970a700, 070361b are the work under review — `git show --stat` each, `git diff fe0ee37..HEAD` for the whole diff).

The work: the studio's top shelf became a VS Code-style menu bar over a portable package. Plan (approved, reviewed): /Users/ryanpederson/NewDev/CreatorStudioUI/references/reviews/2026-09-04-menubar/plan.md — its "Verification" and "Build order" sections are the promises to check delivery against. Build under review: /Users/ryanpederson/NewDev/CreatorStudioUI/packages/menubar/ (README.md is the package's contract and extraction procedure), /Users/ryanpederson/NewDev/CreatorStudioUI/apps/studio/src/app/, /Users/ryanpederson/NewDev/CreatorStudioUI/packages/contracts/src/lib/preferences.ts, /Users/ryanpederson/NewDev/CreatorStudioUI/tools/src/verify/{lib,cockpit,menubar,all}.mjs, /Users/ryanpederson/NewDev/CreatorStudioUI/AGENTS.md, /Users/ryanpederson/NewDev/CreatorStudioUI/references/friction-notes.md (top six entries dated 2026-09-04).

YOUR FRAMING — what is MISSING, and whether the record is honest. Hunt for: plan promises not delivered or silently narrowed (compare the plan's unit and browser assertion lists to the specs and to tools/src/verify/menubar.mjs line by line); behaviours a writer would hit that nothing tests (e.g. shortcuts while typing in a field once an editor exists, the top shelf collapsed then a shortcut, theme on a fresh profile, two menus' portals stacking, long labels, keyboard-only users reaching the toggles at the right); portability that is claimed but would fail if someone actually copied packages/menubar out — check the README's extraction steps against package.json exports, tsconfig assumptions, the `./shortcuts.js` import style, the story's `@ladle/react` import, the spec's dependence on the shared vitest setup, whether `@testing-library/user-event` is declared anywhere the package can see; the CSS contract table vs the actual properties in menubar.css (every row real, every property documented, fallbacks matching); dark-theme correctness of the studio bridge in apps/studio/src/styles.css against packages/tokens/src/tokens.css; a11y gaps (the spacer span, the disabled seams' discoverability, screen-reader announcement of check state vs the ✓ glyph, focus visibility on rows); docs drift — every sentence added to AGENTS.md and friction-notes.md must be true of the code as it stands (quote the code); accepted risks that are stated vs ones that are hidden; the review record itself (references/reviews/2026-09-04-menubar/) — what a later session would need that is not there.

Quote file:line for every finding. You may run `pnpm exec vitest run` inside a package dir (read-only otherwise).

OUTPUT (markdown, to stdout only): `## Findings` — each with severity **Material** (a missing thing that changes the shipped feature, a false claim, or a portability break) or **Minor**, `path:line`, what is missing, the evidence, the fix you would make; then `## Clean passes` — exactly what you checked and found complete (a bare "looks good" is a failed review); then `## Verdict` one line.
