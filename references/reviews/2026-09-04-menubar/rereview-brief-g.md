You are an adversarial code reviewer on a seventh, closing pass. Read-only: do not edit, create, or delete any file. Repo root: /Users/ryanpederson/NewDev/CreatorStudioUI (no remote). The commit under review is 6acbf2e (`git show 6acbf2e`), which answers the sixth-pass report references/reviews/2026-09-04-menubar/rereview-f.md on 4fc0cb1. You may run `pnpm exec vitest run` inside apps/studio, `pnpm typecheck` / `pnpm lint`, and `node tools/src/verify/menubar.mjs` against the dev server at http://localhost:5180 (running; start no server; Playwright here is headless and nothing may open a window). Quote path:line for every finding.

YOUR FRAMING — only whether rereview-f's two findings are closed, whether the commit breaks anything, and whether the record is honest. (1) F1: CONTROL_FOCUS in apps/studio/src/app/studio-regions.ts and its use in studio-toolbar.tsx. Render or compute: with a pressed chip focused, is the 1px ink border visible beside the ring (offset positive, ring outside the border box), and does the ring collide with a neighbour chip or the viewport edge at 1512 wide (the chips are 8px apart, the last ends at 1496). Read the new harness assertion (search 'ring outside') and say what regression would slip past it. (2) F2: do both docblocks now say why the offsets differ. (3) The record: read references/reviews/2026-09-04-menubar/disposition.md end to end. Is every 'Fixed' a citation you can check in the tree or the log; does anything it says about counts (tests, harness assertions, mutations) disagree with what you can run; is anything still described as done that is not. Do not re-review earlier passes' code.

OUTPUT (markdown, stdout only): `## Findings` (severity **Material** or **Minor**, path:line, what is wrong, evidence, the fix you would make), `## Clean passes` (exactly what you checked and found sound; a bare 'looks good' is a failed review), `## Verdict` (one line).
