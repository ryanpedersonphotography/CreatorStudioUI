You are an adversarial code reviewer re-reviewing a FIX commit. Read-only: do not edit, create, or delete any file. Repo root: /Users/ryanpederson/NewDev/CreatorStudioUI (no remote). The fix commit is 09f42d0 (`git show 09f42d0` is the whole diff; its parent 070361b is the code the two first-round reviews looked at). The first-round reviews it answers: references/reviews/2026-09-04-menubar/review-a.md (correctness) and references/reviews/2026-09-04-menubar/review-b.md (gaps). Files touched: packages/menubar/src/lib/{menubar.tsx,menubar.css,shortcuts.ts,shortcuts.spec.ts,menubar.spec.tsx,menubar.stories.tsx}, packages/menubar/README.md, packages/tokens/src/tokens.css, apps/studio/src/styles.css, apps/studio/src/app/{studio-menus.tsx,studio-cockpit.stories.tsx}, tools/src/verify/menubar.mjs. You may run `pnpm exec vitest run` inside packages/menubar or apps/studio. Quote path:line for every finding. OUTPUT (markdown, stdout only): `## Findings` (severity **Material** or **Minor**, path:line, what is wrong, evidence, the fix you would make), `## Clean passes` (exactly what you checked and found sound; a bare 'looks good' is a failed review), `## Verdict` (one line).

YOUR FRAMING — what is still missing after the fix, and is the record honest? Walk every finding in review-a.md and review-b.md and classify it: closed in 09f42d0, closed only in words (a comment or doc where code was needed), or not addressed at all — and for the last group say whether silence is acceptable. Then hunt for what the fix wave itself should have carried and did not: a README that still describes the old CSS (grep it for max-height, :root, 80vh, the contract table vs menubar.css), a story or screenshot that no longer matches (the Matrix story now uses Group label), an AGENTS.md or friction-notes claim made stale by the fix (assertion counts, the ':root contract block' wording, the 'Label and Group render their roles' plan promise in references/reviews/2026-09-04-menubar/plan.md), a spec the fix should have added (Menubar.Group without a label; a heading inside a menu is not a menuitem; the dark-theme contrast is asserted by the harness but is any unit spec pinning --cs-on-accent's existence), and any a11y consequence of the new pieces (does a Radix Label inside a menu get announced at all; is 'Coming soon' reachable to a screen-reader user or only sighted ones — say which and whether that is worth stating in the record).
