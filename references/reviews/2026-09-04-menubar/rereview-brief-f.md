You are an adversarial code reviewer on a sixth, closing pass. Read-only: do not edit, create, or delete any file. Repo root: /Users/ryanpederson/NewDev/CreatorStudioUI (no remote). The commit under review is 4fc0cb1 (`git show 4fc0cb1`), which answers the fifth-pass report references/reviews/2026-09-04-menubar/rereview-e.md on c90ded1. You may run `pnpm exec vitest run` inside apps/studio, `pnpm typecheck` / `pnpm lint`, and `node tools/src/verify/menubar.mjs` against the dev server at http://localhost:5180 (running; start no server; Playwright here is headless and nothing may open a window). Quote path:line for every finding.

YOUR FRAMING — only whether rereview-e's two findings are closed, and whether the commit breaks anything. (1) E1: read the `drawn` clause in tools/src/verify/menubar.mjs (search 'drawn') and the pressedState assertion. Does it catch `border-style: none`, `border-width: 0`, and a dropped `border` utility? Can it be fooled by a border drawn on one side only, or by a 0.5px hairline (parseFloat of '0.5px'), or by `border-color: transparent` (already covered by alpha?), and are any of those plausible authoring paths? Is anything about the text probes changed by the refactor (they must still return the same ratios: 5.31/5.85/5.85 light, 7.30/6.34/6.34 dark). (2) E2: LANDMARK_FOCUS on RegionButton in apps/studio/src/app/studio-toolbar.tsx: read apps/studio/src/app/studio-regions.ts for what it is, confirm it is a focus-visible ring in the project's colour and not something meant only for landmarks (tabindex -1 sections), confirm a pressed chip and a focused chip are now distinguishable (the ring is ember, the outline is ink), and confirm no harness or spec asserted the old UA ring. (3) Anything the commit broke.

OUTPUT (markdown, stdout only): `## Findings` (severity **Material** or **Minor**, path:line, what is wrong, evidence, the fix you would make), `## Clean passes` (exactly what you checked and found sound; a bare 'looks good' is a failed review), `## Verdict` (one line).
