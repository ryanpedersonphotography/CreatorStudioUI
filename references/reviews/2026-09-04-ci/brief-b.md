You are reviewing CI and a visual-regression baseline that were just added to the repo at
/Users/ryanpederson/NewDev/CreatorStudioUI. You hunt for what is MISSING, FLAKY, UNMAINTAINABLE, or
UNSAFE: the things that will make this gate lie, rot, or get switched off within a month. A bare
"looks good" is a failed review; a clean pass must name what you checked and how.

Context. The gate is `pnpm verify` (typecheck, lint, token lint, unit tests, Ladle build, app build,
two Playwright property harnesses on the built bundle, then `pnpm visual`). The visual baseline is
`@playwright/test` screenshots of every Ladle story and four studio views at 1512×982 @1×, compared
pixel for pixel on CI only; baselines are Linux renderings regenerated by a dispatch workflow that
commits to a branch. Main is protected by a ruleset requiring the `verify` check. A solo developer
with an AI pair does the commits.

Read the artifacts at their paths, not any summary:
- /Users/ryanpederson/NewDev/CreatorStudioUI/.github/workflows/ci.yml
- /Users/ryanpederson/NewDev/CreatorStudioUI/.github/workflows/visual-baselines.yml
- /Users/ryanpederson/NewDev/CreatorStudioUI/.github/ruleset-main.json
- /Users/ryanpederson/NewDev/CreatorStudioUI/tools/src/visual/ (config, three specs, baselines/)
- /Users/ryanpederson/NewDev/CreatorStudioUI/AGENTS.md (Conventions, Where things live),
  /Users/ryanpederson/NewDev/CreatorStudioUI/references/friction-notes.md (last three entries),
  package.json scripts, .gitignore
- The real runs: `gh run list --limit 10`, `gh run view <id> --log`; the PNGs the bot committed
  (open them with the Read tool and judge whether each is a picture of the thing it names).

Targets, in priority order. For each, say what is missing and what you would add, with a cost:
1. Flakiness. Every source of non-determinism in the screenshots: fonts (system stack on Linux),
   `document.fonts.ready`, `networkidle`, Radix portals and positioning, focus rings after a click,
   scrollbars, the 150ms colour transitions, Ladle's own chrome or spinner, timing between the two
   web servers, `fullyParallel` with a shared server. Judge whether `maxDiffPixels: 0` is sustainable
   on a pinned runner or whether a tolerance is needed, and say which.
2. Coverage gaps that matter for THIS app: dark theme for stories, the collapsed rails and strips,
   the 2× rendering, the compact states, submenus, focus states. Which of these should be a baseline
   now and which would just be noise?
3. Maintenance. When a developer legitimately changes the UI, walk the exact steps they must take
   from the docs alone (AGENTS.md + workflow headers). Is anything unstated, wrong, or a trap (the
   bot push not triggering CI; opening the PR before vs after the dispatch; the darwin gitignore)?
4. Supply chain and permissions: actions pinned to major tags rather than SHAs; `contents: write` on
   the baselines job; the bot identity; artifact retention (default 90 days) and size; Actions
   minutes for a private repo; whether `cancel-in-progress` on main is right.
5. Process fit for a solo developer: the ruleset forces every change through a pull request. Is that
   the right default here, and what is the cheapest escape hatch that does not silently weaken the gate?
6. What the property harnesses already cover that the baseline duplicates, and what the baseline
   covers that nothing else does. Is the split sane?

You may run read-only commands and `pnpm nx run tools:lint`. Do not start any server or run `pnpm visual`
or `pnpm verify:ui` (the other reviewer holds ports 61000 and 5181; judge from the files, the committed
PNGs and the GitHub run logs). Do not
edit any file, do not push, do not create or merge anything on GitHub, do not run `gh api` with a
method other than GET. Work in /Users/ryanpederson/NewDev/CreatorStudioUI; use absolute paths.

Known before you start: GitHub refused the ruleset on this private repository (HTTP 403, "Upgrade to
GitHub Pro or make this repository public"), so `.github/ruleset-main.json` is kept for when it can be
applied and the check is advisory; AGENTS.md and the ci.yml header state the merge procedure. Assess that
procedure and its escape hatches rather than the ruleset's enforcement.

Report in markdown: `## Findings` (each: severity Material/Minor, file:line or log line, what is
missing or wrong, the fix you would make with its cost), `## Clean passes` (what you checked, how,
with the evidence), `## Verdict` (one sentence).
