Read-only pass complete. Report follows.

## Findings

1. **Material — the disposition marks B5 "Fixed" by a fix that was never made, and the one workflow a reader can reach on `main` is still the defective round-1 copy.** The B5 row says "The workflow files were seeded on `main` (`78a6637`) so `--ref` dispatch works." `git ls-tree -r --name-only origin/main -- .github` returns exactly one path: `.github/workflows/visual-baselines.yml`. `ci.yml` is not on `main` — it is added by this branch — and `78a6637`'s own subject is "ci: seed the **visual-baselines** workflow on main". The code hedges correctly (`.github/workflows/ci.yml:21`, "also works once this file is on main") and so does the friction note; only the disposition, the record a later session reads first, claims otherwise. Worse, `AGENTS.md:119` sends the contributor to the visual-baselines header for the commands, and the copy on `main` (`git show origin/main:.github/workflows/visual-baselines.yml`) is lines 5–6 `git switch -c visual/<why>` — the wrong dispatch recipe B4 found — plus line 22's silent `if: github.ref != 'refs/heads/main'` skip A3 found, plus unpinned actions at line 26. Three findings the table marks Fixed are, for anyone reading `main` today, still on the page they were pointed at.

2. **Material — the repo's own footgun record now says a committed baseline is a defect.** `references/friction-notes.md:179` states the B6 symptom as fact: "the first `stories-dark` baselines were byte-identical to the light ones." `cmp` across all seven pairs shows `stories-dark/menubar--dark-linux.png` is still byte-identical to `stories/menubar--dark-linux.png` (both 15475 B) — legitimately, because `packages/menubar/src/lib/menubar.stories.tsx:109` sets `colorScheme: 'dark'` inline on the wrapper and line 118 portals the menus into it, so the story renders dark under either project. That explanation exists in exactly one place: the untracked `disposition.md`. Nothing the next contributor sees — not the story's docstring at lines 97–102, not `stories.visual.mts:8-12`, not AGENTS.md — records that one identical pair is by design. The next person who runs the same `cmp` the author used to validate will read the friction note, conclude the theme fix regressed, and go re-fix a working thing.

3. **Material — the new orphan check is blind to an orphan directory, which is the same defect A2/B3 raised, one level up.** `tools/src/visual/baselines.mjs:52` iterates `Object.keys(expected())` to decide which folders to scan, and `expected()` at lines 43–46 returns only `stories`, `stories-dark`, `studio`. Delete or rename a Playwright project — `stories-dark` → `dark` is the obvious churn, and adding the project is precisely what this round did — and its seven PNGs stop being enumerated at all: `difference()` reports `{missing: [], orphans: []}`, `manifest.visual.mts:16` passes, `--prune` removes nothing, and ~95 KB of pictures nothing names sits in the tree permanently. The check answers "is every expected file present" but not "is every present file expected," which was B3's whole point. The remedy is the same shape as what's already there: `readdirSync(baselines, {withFileTypes: true})` and flag any directory not in `expected()`.

4. **Material — the project's documented dev workflow and the new last step of `pnpm verify` fight over port 61000.** `AGENTS.md:171` instructs that "Long-running processes (dev server, test watcher, **Ladle**) get their own pane," and `pnpm stories` is `ladle serve`, which `.ladle/config.mjs:8` pins to `port: 61000`. `pnpm verify` now ends in `pnpm visual` (`package.json`), and `tools/src/visual/playwright.config.mts:84-89` starts `ladle preview -p 61000` with `reuseExistingServer: false`. With the Ladle pane up, the run dies at the final step — either with "is already used, make sure that nothing is running on the port/url" (`node_modules/playwright/lib/runner/index.js:848-851`) if the dev server answers `/meta.json`, or with a 30-second URL-poll timeout if it does not. Neither message names the pane. The behaviour is documented only in the config file's own header at lines 15–16; `AGENTS.md` mentions neither port, so a contributor who follows the house workflow burns a full typecheck-lint-test-build cycle to reach an error the project already knew about.

5. **Minor — three documents name three different homes for the merge procedure.** `references/friction-notes.md` (the ruleset entry) says "the merge procedure in **AGENTS.md** is the guard"; `AGENTS.md:122` says "the two commands are in the **`ci.yml` header**"; `disposition.md`'s accepted-risks list says "the merge procedure in the **`ci.yml` header** is the guard." So the friction note points at a file that forwards to a third file, which — per finding 1 — is not on `main`. The procedure is written once and referenced twice, but the two references disagree about where the once is.

6. **Minor — the "kept aligned by hand" accepted risk has a one-token remedy that wasn't taken.** `package.json`'s `verify` script is `typecheck && lint && lint:tokens && test && stories:build && build && verify:ui --preview && visual` — no `nx sync:check`, which `.github/workflows/ci.yml:76` runs. `AGENTS.md:128` handles it by asking a human to remember `pnpm nx sync` after adding a project. That's the one CI-only step whose trigger is an action the local developer takes, so it is the divergence most likely to produce a red the contributor could not have seen; appending `&& pnpm exec nx sync:check` to `verify` closes it without any of the mechanical step-list matching A7/B10 correctly declined.

7. **Minor — the A6 accepted risk credits SHA-pinning with mitigating something it doesn't reach.** The row reads "its actions are pinned by commit SHA (B11)" as one of three reasons the write token is tolerable. Pinning constrains the four `uses:` steps; it does nothing about the branch-controlled code that runs *between* them under `contents: write` — `pnpm install --frozen-lockfile` (`visual-baselines.yml:51`) executing lifecycle scripts, and `.ladle/vite.config.mts` and the studio vite config executing during lines 53–54. The "Never regenerate on main" guard at lines 34–38 checks the dispatch ref, not the token's scope, which is repo-wide. The risk is still fine to accept for a one-contributor private repo — that reason is the honest one and it's stated — but the remedy that would remove it isn't on the record: split into a `contents: read` job that uploads the PNGs as an artifact and a second job that downloads and commits while running no repo code.

8. **Minor — the merge check can go green while a red run sits on the head commit.** `ci.yml:10` is `gh run list --branch <b> --workflow CI --status success --limit 1 --json headSha`. It filters to successes and takes the newest, so a later failing run on the same SHA is invisible: dispatch CI by hand (line 21 and the `workflow_dispatch` trigger at line 30 both invite this), have it fail on a flake, and the earlier `pull_request` success still matches `headRefOid`. The query also doesn't filter `--event`, so a `workflow_dispatch` success satisfies it — the exact run type line 13 warns "tests the branch tip, not the branch merged with main." Adding `--event pull_request` and reading the newest run regardless of status would make the check say what the header means.

9. **Minor — one row of the runs table names the wrong commit.** `disposition.md` lists "33942098972 | visual-baselines on `218ca00`". `GET /actions/runs/33942098972` returns `head_sha` `5928a91b5a9b035d900a0323062e8b23d456b425` — the merge commit "Merge branch 'main' into ci/verify-gate", not `218ca00`. The run table is the audit trail for a gate whose whole premise is binding green to a specific commit; a wrong SHA in it is the one kind of error that table cannot afford.

10. **Minor — the review record isn't in the pull request.** `git ls-files references/reviews/2026-09-04-ci/` returns only `brief-a.md`, `brief-b.md`, `review-a.md`, `review-b.md`. `disposition.md` and both round-2 outputs are untracked, so `AGENTS.md:187`'s "`references/reviews/` (review-gate records)" would merge carrying the round-1 reviews and none of what was decided about them. The file was also being written during this review (mtime 23:57 filled in the `LINTFIX_RESULT` placeholder), and `RE_REVIEW_PENDING` at line 54 is still open — expected mid-flight, but the commit has to happen for the record to exist.

## Clean passes

**Dark coverage is complete, and I checked it file by file rather than by count.** `dist/ladle/meta.json` lists seven stories; `ls` shows seven `-linux.png` in `baselines/stories/` and seven in `baselines/stories-dark/`, with no dark-only file and no missing dark twin. `cmp -s` on all seven pairs: six differ (byte sizes diverge in both directions, which rules out a systematic light copy), one is identical, and that one is explained by `menubar.stories.tsx:109` as covered in finding 2. `expected()` at `baselines.mjs:45` returns the same story list for both projects, so `manifest.visual.mts` would fail on any gap.

**The B6 guard actually guards the failure mode it was written for.** I traced the assertion at `stories.visual.mts:28` against the token selectors: `packages/tokens/src/tokens.css:89` gives `color-scheme: dark` under `[data-theme='dark']`, and lines 101–102 give it under `@media (prefers-color-scheme: dark)` scoped to `:root:not([data-theme='light'])`. If Ladle regressed to stamping `data-theme="light"` — the original bug — the attribute selector misses and the media rule is excluded, `html` computes `color-scheme: light`, and `toHaveCSS` fails before the shutter. The check is not decorative.

**The merge procedure works; I ran both documented commands verbatim.** `gh run list --branch ci/verify-gate --workflow CI --status success --limit 1 --json headSha --jq '.[0].headSha'` and `gh pr view 1 --json headRefOid --jq .headRefOid` both return `22f74bcb6d4efa0c8711c2a4aaf6f8f500dda276`, matching local `HEAD`. `gh pr checks 1` still returns `GraphQL: Resource not accessible by personal access token`, exactly as `AGENTS.md:122` and the ruleset friction note say.

**CI is green on the branch tip.** `GET /actions/runs?branch=ci/verify-gate` shows run `33945785146` on `22f74bc`, event `pull_request`, `status=completed conclusion=success` — the run that exercises `nx sync:check`, the dark project and the manifest test together. The preceding failures line up with the disposition's account: `33943280968` and `33943368076` failed on `16326d1`/`f19b828`, `33943640872` failed on `a64d3a4`, `33943658834` was cancelled.

**The held-run friction note is accurate, and the asymmetry it describes is visible in the data.** Across the seven CI runs, the two with `actor=github-actions[bot]` are `33943368076` (`f19b828`) and `33943658834` (`9bcd3b6`) — both bot pushes to an already-open PR, both the ones the disposition records as held and approved. `33942218972` on the bot commit `de13357` shows `actor=ryanpedersonphotography` and ran unheld, which is the note's own carve-out: a PR opened after the dispatch gets its run from the `opened` event.

**`ci.yml`'s ordering claim is precise and holds.** Line 3 says "the rest of the gate is root scripts, run in the same order as the local script." Against `package.json`, the root scripts appear in CI as `lint:tokens` → `stories:build` → `verify:ui --preview` → `visual` (lines 78–84), the same relative order as `verify`. The two CI-only insertions — `nx sync:check` (76) and `nx build studio` (81) — are both named or implied in `AGENTS.md:115-117`. The header does not overclaim.

**The B2 fix is present in both specs and the prune ordering is right.** `support.mts:26-32` awaits `document.fonts.ready` inside the page and then two nested `requestAnimationFrame` callbacks, returning `undefined` (which also closes B12); `stories.visual.mts:29` and `studio.visual.mts:21,38` both call it, the latter after the View click and menu wait. In `visual-baselines.yml`, `stories:build` (53) and `nx build studio` (54) precede the prune (57), so `baselines.mjs` reads a fresh manifest; `--prune` then exits 0 on `missing` by design (`baselines.mjs:97`) because the next step regenerates them, and the un-flagged confirm run at line 65 re-runs the manifest test for real.

**`test.info().project.use.colorScheme` resolves correctly under either merge semantics.** `stories.visual.mts:22` falls back to `'light'`; the `stories` project inherits `colorScheme: 'light'` from `playwright.config.mts:48` and `stories-dark` sets `'dark'` at line 73, so the URL parameter and the emulated media agree in both projects whether or not Playwright folds top-level `use` into `FullProject.use`.

## Verdict

The engineering behind the round-1 fixes is sound — the dark project is real coverage, the theme assertion guards its own failure mode, the orphan check works for the churn it was built for, and the gate is green on the tip — but the words have fallen behind the code in ways that will mislead the next reader: the disposition claims a seeding that never happened, the friction note now describes a committed baseline as a bug, and the only workflow reachable on `main` still carries three defects the table calls Fixed. Nothing here blocks the merge on correctness grounds; the four Material items are all repairs to prose, one twenty-line widening of the orphan check, and one line in AGENTS.md about port 61000.
