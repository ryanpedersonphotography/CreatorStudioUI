Three claims in the disposition don't survive checking, and one of them is a row marked Fixed whose fix never happened.

## Findings

1. **Material — B5 is marked Fixed against a commit that is this branch's diff base and seeded only the *other* workflow.** The row reads "Fixed. The workflow files were seeded on `main` (`78a6637`) so `--ref` dispatch works." `git ls-tree -r --name-only origin/main -- .github` returns exactly one path: `.github/workflows/visual-baselines.yml`. `ci.yml` is not on main. `78a6637` is main's tip and the base of the diff I was asked to read (`git rev-parse origin/main` → `78a6637`), its subject is "ci: seed **the visual-baselines** workflow on main" — singular — and review A had already cited it as pre-existing in its own finding 3, before the fix round began. `gh run list --workflow CI --event workflow_dispatch` returns nothing: CI has never been dispatched and cannot be, because `workflow_dispatch` requires the trigger to be present on the default branch. What was actually taken is review B's *second* option — the honest hedge at `.github/workflows/ci.yml:21` ("`gh workflow run CI --ref <b>` also works once this file is on main") and the matching friction note. That is a fine resolution; the row describes a different one. A session reading the disposition will believe the hatch is armed today, reach for it on the one PR that most needs it, and get a 422.

2. **Material — `--prune` cannot tell a deliberate deletion from a story-discovery failure, and commits either.** `.github/workflows/visual-baselines.yml:56-57` runs `node tools/src/visual/baselines.mjs --prune` before regeneration, and `tools/src/visual/baselines.mjs:81-97` unlinks every PNG absent from `expected()`, which is whatever `dist/ladle/meta.json` happens to list (`baselines.mjs:28-46`). The only sanity check is total emptiness (`:37-38`); a partial shortfall passes. Ladle's discovery is three hand-written globs with an explicit depth caveat (`.ladle/config.mjs:5-8`: `packages/*/src/**` and `packages/*/*/src/**`), so a package moved one level deeper, or a story file that fails to be picked up, silently leaves `meta.json`. Prune then deletes both of that story's baselines, `--update-snapshots changed` writes nothing in their place, and `tools/src/visual/manifest.visual.mts:16` compares the shrunken expected set against the shrunken disk set and passes. The bot commits the deletions under "regenerate baselines on ubuntu-24.04", and CI on that commit is green. This is precisely the A2/B3 failure mode — coverage shrinking with nothing turning red — reintroduced by the step added to close it, except now it is automatic and committed. I did not reproduce it (running the workflow is out of scope); the argument is structural. The cheap guard is for prune to refuse when the discovered story count falls below the committed baseline count rather than deleting on trust.

3. **Material — the shared view list stops names drifting but not behaviour, and the code comment claims otherwise.** `tools/src/visual/baselines.mjs:3-5` states "the studio views are named here so the spec and this list cannot drift." The names cannot; what the spec does with them can. `tools/src/visual/studio.visual.mts:26-32` parses `<view>-<scheme>` off the shared list and then hardcodes `if (view === 'view-open')` as the only behaviour keyed to a name. Rename `view-open-light`/`view-open-dark`, or add a fifth entry such as `inspector-open-light`: prune removes the old PNGs, the spec photographs the plain cockpit under the new filename, the manifest check passes because both sets moved together, and the only baseline of a rendered studio menu surface — the reason B2 and B6 exist — is quietly replaced by a duplicate of `cockpit-dark`. Nothing goes red. The A2/B3 fix covers the story half completely, because story keys carry no behaviour; the studio half is where the coupling lives and it is unchecked.

4. **Minor — B8's fix landed in the config docstring and not in AGENTS.md, which is one of the two places B named.** `tools/src/visual/playwright.config.mts:3-5` and `:53-56` now carry `threshold: 0.2` and the reason. `AGENTS.md:114` still reads "compared on CI against `tools/src/visual/baselines` with no differing pixel allowed" — the same claim B raised, reworded from "pixel for pixel" but not corrected, with no mention of the per-pixel threshold. The disposition's B8 row closes the gap by declaration rather than by edit: "AGENTS.md's 'no differing pixel' means no pixel beyond that threshold" is an interpretation AGENTS.md nowhere states. B's fix was "correct both comments"; one of them was corrected.

5. **Minor — the runs table misreports the head commit of the first baselines run.** The table lists "33942098972 | visual-baselines on `218ca00`". `gh run view 33942098972 --json headSha` returns `5928a91` — the merge commit, not `218ca00`, which is its parent. The consequence is confined to the audit trail, but the runs table *is* the audit trail, and it is the thing a later session will use to reconstruct which commit produced which images.

6. **Minor — two placeholders are still live in the record, one of them where the only green run on the branch tip belongs.** `disposition.md` carries `RE_REVIEW_PENDING` (this round fills that) and, in the last runs-table row, "33945785146 | CI on `22f74bc` | LINTFIX_RESULT". That run concluded **success**: `gh run view 33945785146` reports all fourteen steps green, with `pnpm visual` logging "Running 19 tests using 2 workers … 19 passed (16.9s)". Until the cell is filled, the record nowhere states that the branch tip is green — which is the single fact the merge procedure at `ci.yml:10-11` turns on.

7. **Minor — B2's fix is in both specs, as the row says, but the precondition it depends on is established in only one.** `settle()` (`tools/src/visual/support.mts:26-32`) is called from `stories.visual.mts:29` and from `studio.visual.mts:21` and `:38`. In the studio spec the two frames are counted from `getByRole('menu').waitFor()` at `:37`, i.e. from the menu being in the DOM. In the stories spec they are counted from `#ladle-root > *` at `:27`, which the `<Menubar>` bar itself satisfies. `menubar--matrix` (`menubar.stories.tsx:69`) and `menubar--dark` (`:119`) both open a portalled Radix menu on load and live only in the stories spec — the case B called "the worst case: no interaction to absorb the latency." The runs are green because `toHaveScreenshot`'s stabilise-until-two-identical-frames loop absorbs it, not because the wait guarantees it. The related gap: the stories spec never asserts a menu opened, so a `defaultValue` regression photographs a closed bar deterministically rather than failing.

8. **Minor — CI now runs a step `pnpm verify` does not, and the A7 row understates that.** `ci.yml:76` adds `pnpm exec nx sync:check`; `package.json:14`'s verify chain does not include it. `AGENTS.md:115-116` discloses it honestly ("with the Nx targets narrowed to `nx affected` plus `nx sync:check`"), so this is not undocumented — but the disposition row describes the change as "the same gate with the Nx targets narrowed to affected", which is now the weaker half of the truth: a green local `pnpm verify` no longer implies a green CI, and the divergence produces exactly the failure `AGENTS.md:128` leaves to a human to remember. Appending `&& pnpm exec nx sync:check` to `verify` closes it in the one place it is missing.

## Clean passes

**The merge procedure works with this token — I ran both header commands.** `gh run list --branch ci/verify-gate --workflow CI --status success --limit 1 --json headSha --jq '.[0].headSha'` returns `22f74bcb6d4efa0c…` and `gh pr view 1 --json headRefOid --jq .headRefOid` returns the same SHA, so the A1 comparison at `ci.yml:10-11` both executes and currently passes. `gh pr checks 1` does fail — `GraphQL: Resource not accessible by personal access token (node.statusCheckRollup…)` — exactly as `AGENTS.md:122-123` records, and no command in either workflow header depends on check runs. I was sent to find a header command that this token cannot run; there isn't one.

**The whole gate is green on the branch tip, step by step.** `gh run view 33945785146 --json jobs` shows the `verify` job success with every step success: checkout, pnpm, node, install, `playwright install`, `nx-set-shas`, `nx affected -t typecheck lint test build`, `nx sync:check`, `lint:tokens`, `stories:build`, `nx build studio`, `verify:ui --preview`, `visual`. The `pnpm visual` log shows the `[manifest]` test *executing* rather than skipping ("✓ 19 [manifest] › … the committed baselines match the stories and views"), which is the direct evidence that A2/B3's orphan check is live on CI and not silently skipped by `test.skip(!process.env.CI)`.

**B6's dark coverage claim verified by hash, not by assertion.** Comparing `baselines/stories/*.png` against `baselines/stories-dark/*.png` with `shasum`: six of seven differ, and `menubar--dark-linux.png` is byte-identical — exactly the row's "six of seven differ; `menubar--dark` forces its own dark scheme and matches by design." The mechanism holds up: `packages/tokens/src/tokens.css:86-98` puts `color-scheme` on `:root` and on `[data-theme='dark']`, Ladle stamps `data-theme` from the `theme` URL parameter (`stories.visual.mts:23`), and `:28` asserts the computed value, so a scheme that failed to apply is now a failing test rather than a wrong picture.

**All four pinned actions dereference to their commented tags.** I resolved each against GitHub rather than trusting the comment: `actions/checkout` v7.0.1 → `3d3c42e5…`, `actions/setup-node` v6.5.0 → `24997072…`, `actions/upload-artifact` v4.6.2 → `ea165f8d…`, and `pnpm/action-setup` v6.0.10, which is an annotated tag, dereferences through `git/tags` to `0977fd99…`. All four match `visual-baselines.yml:40,42,46,82`. `retention-days: 7` is on both upload steps (`ci.yml:94`, `visual-baselines.yml:91`).

**The treeless-clone diagnosis and its fix are both right.** `a64d3a4` removes `filter: tree:0` while keeping `fetch-depth: 0` and `persist-credentials: false` (`ci.yml:51-54`). That combination is coherent: a promisor remote needs credentials for lazy tree fetches and had none, while `nrwl/nx-set-shas` authenticates to the Actions API with `github.token` and never touches git credentials, and `nx affected`'s diff now has every tree locally. The comment at `:49-50` states the reason accurately, and run 33945785146 confirms the step passes.

**A3, A5, A4, B4, B7 and B12 all landed where they were asked for.** The main refusal is a failing first step rather than a job-level skip (`visual-baselines.yml:34-38`); `cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}` is at `ci.yml:40`; the Confirm comment is reworded to "Proves the pictures are stable, not that they are right" (`visual-baselines.yml:64`); the dispatch recipe now says "the branch that carries the UI change" in *both* the workflow header (`:4-12`) and `AGENTS.md:117-119`; and B12's two halves are both present — `settle()` awaits `document.fonts.ready` inside the page and resolves undefined (`support.mts:27-32`), and `studio.visual.mts:20` carries the `toHaveCSS('color-scheme', …)` line B specified.

**The `token-ok` exemption is as narrow as it looks.** `tools/src/lint/check-tokens.mjs:103` is `if (raw.includes('token-ok')) return;` — line-scoped, skipping every rule for that one raw line and nothing else. The line it exempts, `menubar.stories.tsx:112`, contains only `minHeight: '100dvh',` and its reason, so it cannot mask a colour literal or a stray primitive elsewhere in the file. `pnpm lint:tokens` is green on the HEAD run.

**No stale module reference survives the `errors.mts` → `support.mts` rename.** Grepping the working tree for `errors.mjs`/`errors.mts` outside `node_modules`, `.git` and `dist` returns hits only in the round-1 review documents and the Nx daemon log — no import, no config, no workflow.

## Verdict

The code is in better shape than the record of it: every fix I could execute or observe behaves as claimed, the branch tip is genuinely green with the orphan check running rather than skipping, and the merge procedure works with the token this repo actually has. What does not hold is one row marked Fixed on a seeding that never happened (B5), a prune step that turns any story-discovery shortfall into a committed loss of coverage the manifest check then ratifies, and a shared-list comment that promises the studio spec cannot drift when the one behaviour keyed to a view name is an unchecked string compare.
