# CreatorStudioUI

Project shape, intent, and conventions live in **AGENTS.md** — read it first. This file only adds
harness notes.

- Sessions here run inside a Herdr pane (`HERDR_ENV=1`). Read the *Herdr workspace* section of
  AGENTS.md before starting any process or splitting any pane.
- The Agent tool cannot spawn here (iTerm2 split failure); see `references/friction-notes.md`.
